<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Id$ -->
<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
    xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

    xmlns:xi="http://www.w3.org/2001/XInclude" name="ssh_auth_method" required="false"
    DefaultValue="publickey" DataType="SOSOptionStringValueList" extends="auth_method" see="auth_method"
>

    <indexterm index="SSH, ssh_auth_method" display="false"></indexterm>

    <indexterm index="ssh_auth_method, parameter" display="false"></indexterm>
    <indexterm index="parameter, ssh_auth_method" display="false"></indexterm>

    <indexterm index="publickey, ssh_auth_method" display="false"></indexterm>
    <indexterm index="password, ssh_auth_method" display="false"></indexterm>

    <indexterm index="ssh_auth_file, parameter" display="false"></indexterm>
    <indexterm index="parameter, ssh_auth_file" display="false"></indexterm>

    <values>
        <value default="yes">publickey</value>
        <value>password</value>
    </values>

    <!--xi:include href="../notes/Auth_Method.jobdoc" parse="xml">
        <xi:fallback>"../notes/Auth_Method.jobdoc" not found</xi:fallback>
    </xi:include-->
	<note language="de">
		<title>SSH Authentication Method</title>
		<div xmlns="http://www.w3.org/1999/xhtml">
			<p>
			  The use of this element specifies the method to be used for SSH Authentication: <em>publickey</em> (default) or <em>password</em>.
			</p>
			<p>
			  If <em>publickey</em> is used then the <em>ssh_auth_file</em> parameter must be set.
			</p>
			<p>
			  This parameter should be used with either a <em>source_</em> or <em>target_</em> prefix.
			</p>
			<p>Parameter Reference: <a href="https://kb.sos-berlin.com/x/TAOg" target="_blank">SSHAuthentication</a></p>
						</div>
					</note>
					
					<note language="en">
						<title>SSH Authentication Method</title>
						<div xmlns="http://www.w3.org/1999/xhtml">
			<p>
			  The use of this element specifies the method to be used for SSH Authentication: <em>publickey</em> (default) or <em>password</em>.
			</p>
			<p>
			  If <em>publickey</em> is used then the <em>ssh_auth_file</em> parameter must be set.
			</p>
			<p>
			  This parameter should be used with either a <em>source_</em> or <em>target_</em> prefix.
			</p>
			<p>Parameter Reference: <a href="https://kb.sos-berlin.com/x/TAOg" target="_blank">SSHAuthentication</a></p>
		</div>
	</note>
</param>