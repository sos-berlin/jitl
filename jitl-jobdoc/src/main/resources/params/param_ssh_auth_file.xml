<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Id$ -->
<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       name="ssh_auth_file"
       required="false"
       DataType="SOSOptionInFileName"
xmlns:xi="http://www.w3.org/2001/XInclude"
       >

		       <indexterm index="SSH, ssh_auth_file" display="false"></indexterm>

	 <indexterm index="ssh_auth_file, parameter" display="false"></indexterm>
    <indexterm index="parameter, ssh_auth_file" display="false"></indexterm>

    <indexterm index="publickey, ssh_auth_file" display="false"></indexterm>

	 <indexterm index="ssh_auth_file, parameter" display="false"></indexterm>
    <indexterm index="parameter, ssh_auth_file" display="false"></indexterm>


        <note language="de">
		    <title>Schlüsseldatei mit privatem Schlüssel</title>
			<div xmlns="http://www.w3.org/1999/xhtml">
			<p>
			 The value of this parameter specifies the path and name of an account's private key file used for SSH Authentication. 
			 This parameter must be specified if the parameter <em>source_ssh_auth_method = publickey</em> is used.
			</p>
			<p>
			 This parameter should be used with either a <em>source_</em> or <em>target_</em> prefix.
			</p>
			<p>
			 Should the private key file be secured by a passphrase then this must be specified using the <em>password</em> parameter. 
			</p>
			<p>
			 Authentication files are most often stored in the user home directory in a .ssh folder and have to be secured with the proper file permissions. However, any path can be specified for an authentication file with this parameter. 
			 The location of the authentication file has to be accessbible for the account that runs the JADE Client. 
			 For Unix systems file permissions 600 are required for authentication files.
			</p>
			<p>Parameter Reference: <a href="https://kb.sos-berlin.com/x/TAOg" target="_blank">SSHAuthentication</a></p>
          </div>
        </note>
        <note language="en">
		    <title>Keyfile with private Key</title>
			<div xmlns="http://www.w3.org/1999/xhtml">
				<p>
				 The value of this parameter specifies the path and name of an account's private key file used for SSH Authentication. 
				 This parameter must be specified if the parameter <em>source_ssh_auth_method = publickey</em> is used.
				</p>
				<p>
				 This parameter should be used with either a <em>source_</em> or <em>target_</em> prefix.
				</p>
				<p>
				 Should the private key file be secured by a passphrase then this must be specified using the <em>password</em> parameter. 
				</p>
				<p>
				 Authentication files are most often stored in the user home directory in a .ssh folder and have to be secured with the proper file permissions. However, any path can be specified for an authentication file with this parameter. 
				 The location of the authentication file has to be accessbible for the account that runs the JADE Client. 
				 For Unix systems file permissions 600 are required for authentication files.
				</p>
				<p>Parameter Reference: <a href="https://kb.sos-berlin.com/x/TAOg" target="_blank">SSHAuthentication</a></p>
			</div>
        </note>
      </param>